/**
 * co the truyen 1 bien vao condition
 */

let a = 0; //true
let b2 = 'miki'; //false
const dem = (a) => {
    if (a) {
        return (`You're fail`)
    } else {
        console.log(`You're ok`)
    }
}