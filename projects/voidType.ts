/** key word: void
 * Ham khong can tra ve gia tri
 * chi quan tam toi logic of code
 * hay su dung voi function
 */

const sum = (a: number, b: number) => {
    return a + b;
}

const handleLogs = (msg: string): void => {
    console.log(`>>> check ...`, msg)
}